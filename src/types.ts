export type Turma = {
    id: number
    turma_code: string
    disciplina_code: string
    disciplina_name: string
    dest_code: string
    num_vagas: number
    teacher_name: string
    schedule: JSON
}